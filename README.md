# maanox-ui

Easily install UI components in your project from MaanOX's hosted component repo.

## 📦 Installation

Use via `pnpm dlx`, `npx`, or install globally:

```bash
pnpm dlx maanox-ui add "https://ui.maanox.com/r/btn"
